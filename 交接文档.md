# 交接文档 —— 下一次对话从这里开始读

> 写于 **2026-09-21**。仓库 `git@github.com:Gh0stown/AiC-2026.git`，分支 `main`。
> 本文档是给"下一次对话的我"看的：现在到哪了、还差什么、哪些坑别重踩。

---

## 0. 一句话现状

**仿真场景（地图）搭建完成并逐项验证通过**；三大视觉识别任务（红绿灯状态 / 人偶检测
+人群计数 / 车牌 OCR）只有**离线工具**，还没有实时节点；任务二材料（文档/PPT/视频）
没开始。

`HEAD = ff98ead`，和 `origin/main` 一致，工作区干净。

---

## 1. 接手第一步（照这个顺序，10 分钟能跑起来）

```bash
cd ~/桌面/人工智能算法大赛
cat 验收指南.md                  # 怎么启动、怎么验收、常见问题表
source devel/setup.bash          # ★ 每个新终端都要 (用户明确拒绝写进 ~/.bashrc)
roslaunch competition_robot navigation.launch          # 一条命令起全套
./tools/acceptance.sh --quick    # 自动跑 6 项检查 (~4 min)
```

**先别急着加功能**，先把 `./tools/acceptance.sh` 在你那边完整跑一遍（约 12 分钟），
把我给不出的"整条串联"结果确认掉（我这边沙箱会把超过 ~10 分钟的任务杀掉，
所以 6 项我是逐项单独验的，没串起来跑过）。

---

## 2. 已完成（都实测过，不是"写完就算"）

| 能力 | 实测数据 | 在哪 |
|---|---|---|
| 场地世界 + 围墙 | 白线内 4.2×4.2 可行驶；围墙内沿 ±2.176；地板 4.4×4.4；墙高 0.30 m | `competition_arena/worlds/` |
| 地面贴图 | 车道线/斑马线/楼宇轮廓与官方图一致 | `media/materials/textures/arena_floor.png` |
| 红绿灯模型 | 官方尺寸 64×14 cm 灯箱 / Ø8.5 cm 灯珠 / 22 cm 间距 / 双支架 | `models/traffic_light_h/` |
| 红绿灯切换 | **6/6 状态正确**，两盏同步，30 次采样 0 次全灭 | `scripts/traffic_light.py` |
| 红绿灯稳定性 | `z=+0.00200 / 倾角 0.000° / 偏移 0.00000`，10 秒零变化 | 同上 |
| 人偶立牌 | 18 个模型（社区 16 + 非社区 2），摆 10 个 | `models/standee_*/` |
| 车辆 + 车牌 | 3 辆车，车牌贴后备箱 | `models/car_*/` |
| 干净地图 | 几何生成，墙内边界准确 | `tools/gen_map_from_arena.py` |
| 定位 AMCL | 平均位置误差 **0.067 m** | `tools/check_localization.py` |
| 雷达位姿标定 | 激光反推 **2~5 mm**（AMCL 13.4 mm） | `tools/check_laser_pose.py` |
| 巡航 | **7/7 航点**，全程 0 帧压车道线 | `tools/patrol.py` |
| 倒车入库 | 位置 **1.03 cm**，角度 **0.01°** | 同上 |
| 一键验收 | 6 项自动检查 + 通过/失败汇总 | `tools/acceptance.sh` |
| 视觉环境 | `.venv/` torch2.4.1+cpu / ultralytics / hyperlpr3 | `docs/vision_env.md` |
| 车牌 OCR（离线） | 自测 3/3 | `tools/plate_ocr.py` |

---

## 3. 还没做（按"性价比"排序，供下次决定）

| 优先级 | 任务 | 分值 | 说明 |
|---|---|---|---|
| **P0** | **红绿灯状态识别节点** | 视觉 15 | 相机图 → HSV/灯珠定位 → 实时话题。目前只有离线验证脚本 |
| **P0** | **人偶检测 + 人群计数** | 视觉 15 | 完全没有。总决赛还要"12 分·2 街区"计数 |
| **P0** | **车牌 OCR 实时节点** | 视觉 15 | `plate_ocr.py` 能跑，但没和 `/camera/rgb/image_raw` 接起来 |
| **P0** | **识别点位定义** | — | 用户说识别是"开到固定点位原地转向拍照"。点位还没定，也**没算过拍照距离** |
| P1 | 红绿灯**停止线** + 压线判断 | 任务合规 | 比赛要求"红灯期间车身不得越过停止线"。停线还没画 |
| P1 | 建筑物/站房/垃圾桶/电动车/指示牌 | 场景 15 | 提升"高保真"分 |
| P1 | **有障碍物的避障演示** | 导航 20 | 场景里现在只有车道线，避障没法体现 |
| P1 | 一键启动 demo launch | 演示 | `competition_demo.launch` |
| P2 | 技术方案文档 / PPT / 演示视频 | 任务二 50 | 完全没开始 |
| P2 | Cartographer | 加分 | 官方 PDF 两次点名 Gmapping/Cartographer，现在只有 gmapping/karto |

---

## 4. ★ 场地外扩 —— 这是最需要知道的一处结构改动

### 改了什么

为了给"横排双支架红绿灯"（宽 64 cm、两脚间距 59 cm）让位，**围墙从白线退到了白线外
0.10 m**：

| | 之前 | 现在 |
|---|---|---|
| 白线场地（可行驶） | 4.2 × 4.2 m | **4.2 × 4.2 m（没变）** |
| 围墙内沿 | ±2.076 m | **±2.176 m** |
| 地板 | 4.2 × 4.2 | **4.4 × 4.4 m** |
| 外部空地 | 无 | **0.10 m/边**（= 原图外圈到白线的实测 29 px） |

实现：`python3 scripts/build_arena.py --margin 0.10`（默认值已是 0.10）。

### ⚠️ 外扩带来的三个连带影响（不处理就会出错）

1. **ROS 地图必须重出**。`tools/gen_map_from_arena.py` 的 `--inner` 默认已改成
   **2.176**。物理围墙外移而地图不动的话，AMCL 会持续看到 **~10 cm 系统性偏差**。
2. **代价地图内切半径必须收小**。红绿灯是"跨车道的门"，门腿内沿到车道中心只有
   ~0.10 m 余量；`inscribed_radius` 原来是 footprint 自动算出的 0.145，会把门判成
   过不去（表现为 DWA 偶发 `failed to produce path`）。现在
   `costmap_common_params.yaml` 里**显式设 `inscribed_radius: 0.10`**。
3. **墙上那圈 0.30 m 是为了过灯**。早先灯摆在墙外时最外侧透镜的视线在墙处只有
   z=0.315 m，0.5 m 的墙会挡住它。想改回 0.5 m 要重新算遮挡。

### 地图的"真值"从哪来

**图上那些白线是车道线，不是墙。** 车道宽 60~65 cm，正好对上示意图标的 "60cm"。
街区就是车道之间的区域。所有坐标都来自**同一套像素↔世界变换**：

```
x = (px - 29) * 3.4342e-3 - 2.1
y = 2.1 - (py - 31) * 3.4342e-3        # 世界 +x = 图右, +y = 图上
ARENA_PX = (29, 31, 1252, 1254)
```

---

## 5. 场景里到底摆了什么（坐标可直接抄）

### 街区（车道线围出来的区域）

| | x | y | 尺寸 |
|---|---|---|---|
| **A 街区** | [-1.472, -0.452] | [0.847, 1.468] | 1.02 × 0.62 m |
| **B 街区** | [-1.475, -0.407] | [-0.537, 0.197] | 1.07 × 0.73 m |
| 3号停车位 | [1.472, 2.076] | [-0.805, -0.197] | 0.60 × 0.61 m |
| 2号停车位 | [1.472, 2.076] | [-1.423, -0.826] | |
| 1号停车位 | [1.472, 2.076] | [-2.076, -1.448] | |

### 红绿灯（**都是横排双支架**，官方只有横排）

| 灯 | 位置 | yaw | 腿踩在哪 |
|---|---|---|---|
| `tl_top` | (-0.506, **1.779**) | 0（朝 +x） | y = 1.472 / 2.086（顶部车道两条边线） |
| `tl_bot` | (-0.110, **-1.280**) | π/2（朝 +y） | x = -0.4175 / +0.1975（竖车道两条边线） |

**两盏都是"跨在车道上方的门"，机器人从两条腿中间穿过去。** 上灯靠右的腿在墙外
（灯箱高 0.34~0.48 m，高过 0.30 m 围墙，跨墙不打架）。

`tl_bot` 原在 y=-1.456，**正好在 T 字路口上，转弯扫掠区会撞倒它**，已往里挪 0.176 m。

### 人偶立牌（10 个 = 8 社区 + F1 + F2，紧凑成组）

```
A north c01@(-1.01,+1.44) c02@(-0.92,+1.44)      A south c03@(-1.01,+0.88) c04@(-0.92,+0.88)
A west  c05@(-1.44,+1.11) F1 @(-1.44,+1.20)      B north c06@(-0.99,+0.17) c07@(-0.90,+0.17)
B east  c08@(-0.44,-0.22) F2 @(-0.44,-0.13)
```

- 朝向按示意图橙黄箭头（A 是 ←↓↑，B 是 ↑→），**每条边的人偶朝该边的法向**
- 同一方向**紧凑成组、间距 0.09 m**（识别是定点转向拍照，挤在一起才拍得全）
- 摆放工具内置**和红绿灯脚的干涉检查**（立牌是 static、灯不是，压上去会把灯顶歪）

### 车辆 + 车牌

| 模型 | 车牌 | 车位 |
|---|---|---|
| `car_1` | 苏A·B8Q62 | 3号 |
| `car_2` | 鄂D·7B5Q2 | 2号 |
| `car_3` | 苏A·PL12A | 1号 |

车尾朝 -x（朝车道）。车板 34.5×25×5 mm，车牌 9.5×3 cm 贴在**后备箱**（红框中心离车板底
11.65 cm）。

---

## 6. 仓库与提交

- 远端 `git@github.com:Gh0stown/AiC-2026.git`，分支 `main`
- **每个新终端先 `source devel/setup.bash`**（用户明确不要写进 `~/.bashrc`，留着当提醒）
- 提交身份：本仓库局部已设 `Gh0stown <728571239@qq.com>`
  （全局缺 `user.email`，不设会报"作者身份未知"而**提交失败但 push 会"成功"**——很容易漏看）
- `.gitignore` 忽略 `/build/ /devel/ /logs/ /.venv/ /datasets/ /runs/ *.pt *.onnx`
- 大文件（官方素材、视频）不要进仓库

### 关键提交（能看出演进）

```
ff98ead  人偶改紧凑成组 + 修掉立牌把红绿灯顶歪的问题
35e1e77  地图搭建完成: 加一键验收脚本 + 验收指南
1310335  车牌移到后备箱 + 相机提到 1280x960 + 修掉 OGRE 全局贴图名冲突
51719ca  人偶立牌摆进 world
0d19ab3  红绿灯: 场地外扩 0.10m 给灯留地, 彻底消除晃动
0329e48  红绿灯按官方红点重定位: 灯是"跨车道上方的门"
```

---

## 7. ★ 踩过的坑（贵的那几个，别再踩）

### 7.1 OGRE 的材质名**和贴图名**都是全局的

三个车模型都用 `Car/Body` + `Car/Plate` → 互相覆盖，**三辆车显示同一张车牌**。
只改材质名**还不够** —— `car.png`/`plate.png` 同名一样冲突。最终：
**每个模型的材质名和贴图文件名都必须唯一**（`<model>_car.png`），且生成前**清空
textures 目录**（旧文件残留会继续占名）。判断依据：抓帧看到的车牌永远是最先/最后加载
的那张。

### 7.2 立牌/道具的贴图**必须带 alpha 且用 `alpha_rejection`**

比 alpha 混合干净，没有半透明排序问题，立牌边缘就是人物轮廓。

### 7.3 静态道具用 `<static>true</static>`

立牌、车辆都设 static：不会倒、不会被碰到乱飞。**红绿灯不能 static**（要动关节），
于是它用另一套办法（见 7.4）。

### 7.4 红绿灯的晃动：结论是"**不给重力**"

排查过 5 种方案，实测记录：

| 方案 | 结果 |
|---|---|
| 开重力 + 脚压在地面 z=0 | 起步落地弹跳；之后因**零穿透接触**被求解器持续微推，**~0.1 mm/s 匀速慢漂**（23 秒 0.26 m） |
| 开重力 + 加摩擦/加重 | 无改善 |
| 灯珠质量压到 0.001 kg | 与慢漂无关：**起不起切换节点漂移速率完全一样** |
| 关节限位 `[0.005,0.055]` 而默认位置 0 在限位外 | 启动被弹一下 → 晃两下 |
| **关重力 + 整灯离地 2 mm 悬放**（当前） | **完全不动** |

代价：被撞到会被推走且不回来（所以 `tl_bot` 挪离了路口）。

### 7.5 沙箱（我这边）的三个限制 —— 影响我**能验证什么**

1. **相机渲染冻结在启动首帧**，而且取景位置**不确定**（相机插件首帧和 spawn 的竞争）。
   所以我**无法**用"把机器人放到某处再抓图"来验证渲染。
   → 设计类验证改用**离线 PIL 合成**（见 `docs/car_design.png`）。
2. **`$HOME` 只读** → Gazebo 写不了 `~/.gazebo` 就起不来。用
   `HOME=$PWD/.cache/home`（`acceptance.sh` 已自动处理）。
3. **后台任务超过 ~10 分钟会被杀**，且 `/tmp` 每个调用独立 → 长任务拆开跑，
   日志写到工作区内（`.cache/`）。

### 7.6 别的

- `RLException: ... is neither a launch file` = 没 `source devel/setup.bash`
- `check_localization.py` 的参数是 `--duration` 不是 `--samples`
- 一台 shell 里起两个 `roslaunch` 会抢 master 端口 → 先起 `robot_gazebo`，
  等 `/gazebo` 服务就绪，再起别的
- 改了 world **必须重启仿真**才生效

---

## 8. 怎么重出（改过东西之后）

| 改了什么 | 要重跑 |
|---|---|
| 场地/围墙/外扩 | `setup_traffic_lights.py && setup_standees.py && setup_cars.py && gen_map_from_arena.py` |
| 红绿灯尺寸/位置 | `python3 tools/setup_traffic_lights.py` |
| 人偶素材/摆放 | `python3 tools/gen_standees.py && python3 tools/setup_standees.py` |
| 车牌/车辆 | `python3 tools/setup_cars.py` |
| 机器人参数 | `python3 src/competition_robot/scripts/gen_robot.py` |

配置是**唯一真值源**：`config/traffic_lights.yaml`、`config/standees.yaml`、`config/cars.yaml`。
改位置改配置、别改 world（world 是生成的）。

---

## 9. 环境与路径

| | |
|---|---|
| 工作区 | `/home/ghostown/桌面/人工智能算法大赛` |
| 官方素材（**不在仓库里**） | `~/复赛资料/{人员,车辆识别,红绿灯}/` + `复赛任务示意图.png` |
| 视觉 venv | `.venv/`（torch 2.4.1+cpu / ultralytics / hyperlpr3 / opencv 4.10） |
| 主要文档 | `验收指南.md`（怎么跑）、`复赛要求与差距分析.md`（分值拆解）、`PROGRESS.md`（早期排查记录）、`src/competition_arena/docs/traffic_light_model.md`（红绿灯全部理由） |

---

## 10. 我对下一步的建议

1. **先补"识别点位"**。用户说识别是"开到固定点位原地转向拍照"——点位没定，
   后面三个视觉任务都没法收尾。定了点位顺带能算出**拍照距离 → 人偶/车牌占多少像素**，
   直接决定识别方案（模板匹配够不够、要不要上 YOLO）。
2. **再做红绿灯状态识别**（最快闭环）。相机 → HSV 找灯珠 → 判定颜色 → 发话题，
   和已有的 `/traffic_light/state` 可以交叉验证。
3. **然后人偶检测 + 计数**（分值最高、也最能体现"视觉"）。
   素材是 18 张 PNG，**Gazebo 可以自动生成合成数据集**（随机位姿截图 + 自动标注），
   不用手工标。这一步要占 GPU（用户有）。
4. **车牌 OCR** 有现成的 `plate_ocr.py`，缺的只是"从相机图里定位车牌"这一步。
   注意：**相机已提到 1280×960**，0.6 m 处车牌约 198 px（每字 26 px）够 OCR；
   1.2 m 处只有 100 px（每字 13 px）不够——**机器人得贴到车道内侧拍**。
5. **红绿灯停止线**很快，属于合规项，可以插空做。

---

## 11. 需要用户拍板的问题（还没定）

1. 实车相机分辨率是多少？（仿真现在按 1280×960 配的，是按识别需求倒推的）
2. 视觉方案走**纯 OpenCV** 还是**上 YOLO**？
3. 是否装 Cartographer？
4. 识别点位由谁定（组委会给，还是我们自己选）？
5. 报名（9-20 截止）完成了没有？

---

## 12. 已知的、但**没法在我这边确认**的事

- **视觉渲染效果**（红绿灯实际点亮的颜色、贴图有没有歪）—— 我这边相机冻结，
  只能靠"按灯珠位姿反推"和离线合成图；**建议在有 GPU 的机器上人眼过一遍**。
- **整条 `acceptance.sh` 串联运行** —— 6 项我都单独验过，但没串起来跑完过。
- **实车参数**（整车质量是估算的、雷达型号、相机安装高度/俯仰）。
