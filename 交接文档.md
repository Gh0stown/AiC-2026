# 交接文档 —— 下一次对话从这里开始读

> 写于 **2026-09-21**。仓库 `git@github.com:Gh0stown/AiC-2026.git`，分支 `main`。
> 本文档是给"下一次对话的我"看的：现在到哪了、还差什么、哪些坑别重踩。

---

## 0. 一句话现状

**仿真场景（地图）搭建完成并逐项验证通过**；三大视觉识别任务（红绿灯状态 / 人偶检测
+人群计数 / 车牌 OCR）的**识别点位与拍照距离已经算完**（`tools/gen_recognition_points.py`
→ `config/recognition_points.yaml`，报告 `docs/recognition_points.md`），但**还没有实时节点**；
任务二材料（文档/PPT/视频）没开始。

`HEAD = 86ce8f8`（本次识别点位的工作尚未提交），和 `origin/main` 一致。

---

## 1. 接手第一步（照这个顺序，10 分钟能跑起来）

```bash
cd ~/桌面/人工智能算法大赛
cat 验收指南.md                  # 怎么启动、怎么验收、常见问题表
source devel/setup.bash          # ★ 每个新终端都要 (用户明确拒绝写进 ~/.bashrc)
roslaunch competition_robot navigation.launch          # 一条命令起全套
./tools/acceptance.sh --quick    # 自动跑 6 项检查 (~4 min)
```

**先别急着加功能**，先把 `./tools/acceptance.sh` 在你那边完整跑一遍（约 12 分钟），
把我给不出的"整条串联"结果确认掉（我这边沙箱会把超过 ~10 分钟的任务杀掉，
所以 6 项我是逐项单独验的，没串起来跑过）。

---

## 2. 已完成（都实测过，不是"写完就算"）

| 能力 | 实测数据 | 在哪 |
|---|---|---|
| 场地世界 + 围墙 | 白线内 4.2×4.2 可行驶；围墙内沿 ±2.176；地板 4.4×4.4；墙高 0.30 m | `competition_arena/worlds/` |
| 地面贴图 | 车道线/斑马线/楼宇轮廓与官方图一致 | `media/materials/textures/arena_floor.png` |
| 红绿灯模型 | 官方尺寸 64×14 cm 灯箱 / Ø8.5 cm 灯珠 / 22 cm 间距 / 双支架 | `models/traffic_light_h/` |
| 红绿灯切换 | **6/6 状态正确**，两盏同步，30 次采样 0 次全灭 | `scripts/traffic_light.py` |
| 红绿灯稳定性 | `z=+0.00200 / 倾角 0.000° / 偏移 0.00000`，10 秒零变化 | 同上 |
| 人偶立牌 | 18 个模型（社区 16 + 非社区 2），摆 10 个 | `models/standee_*/` |
| 车辆 + 车牌 | 3 辆车，车牌贴后备箱 | `models/car_*/` |
| 干净地图 | 几何生成，墙内边界准确 | `tools/gen_map_from_arena.py` |
| 定位 AMCL | 平均位置误差 **0.067 m** | `tools/check_localization.py` |
| 雷达位姿标定 | 激光反推 **2~5 mm**（AMCL 13.4 mm） | `tools/check_laser_pose.py` |
| 巡航 | **7/7 航点**，全程 0 帧压车道线 | `tools/patrol.py` |
| 倒车入库 | 位置 **1.03 cm**，角度 **0.01°** | 同上 |
| 一键验收 | 6 项自动检查 + 通过/失败汇总 | `tools/acceptance.sh` |
| 视觉环境 | `.venv/` torch2.4.1+cpu / ultralytics / hyperlpr3 | `docs/vision_env.md` |
| 车牌 OCR（离线） | 自测 3/3 | `tools/plate_ocr.py` |
| **识别点位与拍照距离** | **10 个点位**；红绿灯 101 px 灯珠 / 车牌 157 px / 立牌宽 123~205 px | `tools/gen_recognition_points.py` → `config/recognition_points.yaml` |
| OCR 分辨率下界 | 车牌 **70 px 仍 3/3 全对**（每字 8.75 px） | `tools/check_ocr_resolution.py` |
| 车道线/停止线真值 | 17 段（含 **2 条停止线**，官方平面图里本来就有） | `config/lane_lines.json` |
| **最终巡检路线** | 17 站（6 航点 + 10 识别点 + 回起点），**实跑 17/17 到点、0 失败** | `config/recognition_route.yaml` + `docs/recognition_route.png` |
| 一键跑路线 | 起仿真 → 跑整条路线 → 存轨迹 → 重画图 | `tools/run_recognition_route.sh` |
| **到点拍照** | 10 个识别点各拍 3 帧，存 `captures/<时间戳>/`，带真值/灯态/真实位姿 | `tools/capture_points.py` |
| 拍摄总览图 | 一次拍摄拼成一张图，人眼快速过 | `tools/show_captures.py` |

---

## 3. 还没做（按"性价比"排序，供下次决定）

| 优先级 | 任务 | 分值 | 说明 |
|---|---|---|---|
| **P0** | **红绿灯状态识别节点** | 视觉 15 | 相机图 → HSV/灯珠定位 → 实时话题。点位已定，离线脚本已有 |
| **P0** | **人偶检测 + 人群计数** | 视觉 15 | 完全没有。点位已定；总决赛还要"12 分·2 街区"计数 |
| **P0** | **车牌 OCR 实时节点** | 视觉 15 | 缺"从整幅画面里定位车牌"这一步（识别网络已验证够用） |
| ~~P0~~ | ~~识别点位定义~~ | — | ✅ **已完成**：10 个点位 + 像素预算 + 最终路线，仿真实跑 17/17 |
| P1 | 红绿灯**停止线** + 压线判断 | 任务合规 | 停线**本来就在官方图里**（上灯 x=0.309／下灯 y=-0.527），不用画；判断逻辑还没写 |
| P1 | 建筑物/站房/垃圾桶/电动车/指示牌 | 场景 15 | 提升"高保真"分 |
| P1 | **有障碍物的避障演示** | 导航 20 | 场景里现在只有车道线，避障没法体现 |
| P1 | 一键启动 demo launch | 演示 | `competition_demo.launch` |
| P1 | **完整建图过程（视频素材）** | 视频硬性要求 | 现用的干净地图是几何生成的，**视频要的是 SLAM 过程**，得单独录一次 |
| P2 | 相机俯仰/抬高（如果实车允许） | 视觉质量 | 正对拍立牌只能拍到上身（见下），加 10°~15° 俯仰才能拍全身 |
| P2 | 技术方案文档 / PPT / 演示视频 | 任务二 50 | 完全没开始 |
| P2 | Cartographer | 加分 | 官方 PDF 两次点名 Gmapping/Cartographer，现在只有 gammping/karto |

---

## 4. ★ 场地外扩 —— 这是最需要知道的一处结构改动

### 改了什么

为了给"横排双支架红绿灯"（宽 64 cm、两脚间距 59 cm）让位，**围墙从白线退到了白线外
0.10 m**：

| | 之前 | 现在 |
|---|---|---|
| 白线场地（可行驶） | 4.2 × 4.2 m | **4.2 × 4.2 m（没变）** |
| 围墙内沿 | ±2.076 m | **±2.176 m** |
| 地板 | 4.2 × 4.2 | **4.4 × 4.4 m** |
| 外部空地 | 无 | **0.10 m/边**（= 原图外圈到白线的实测 29 px） |

实现：`python3 scripts/build_arena.py --margin 0.10`（默认值已是 0.10）。

### ⚠️ 外扩带来的三个连带影响（不处理就会出错）

1. **ROS 地图必须重出**。`tools/gen_map_from_arena.py` 的 `--inner` 默认已改成
   **2.176**。物理围墙外移而地图不动的话，AMCL 会持续看到 **~10 cm 系统性偏差**。
2. **代价地图内切半径必须收小**。红绿灯是"跨车道的门"，门腿内沿到车道中心只有
   ~0.10 m 余量；`inscribed_radius` 原来是 footprint 自动算出的 0.145，会把门判成
   过不去（表现为 DWA 偶发 `failed to produce path`）。现在
   `costmap_common_params.yaml` 里**显式设 `inscribed_radius: 0.10`**。
3. **墙上那圈 0.30 m 是为了过灯**。早先灯摆在墙外时最外侧透镜的视线在墙处只有
   z=0.315 m，0.5 m 的墙会挡住它。想改回 0.5 m 要重新算遮挡。

### 地图的"真值"从哪来

**图上那些白线是车道线，不是墙。** 车道宽 60~65 cm，正好对上示意图标的 "60cm"。
街区就是车道之间的区域。所有坐标都来自**同一套像素↔世界变换**：

```
x = (px - 29) * 3.4342e-3 - 2.1
y = 2.1 - (py - 31) * 3.4342e-3        # 世界 +x = 图右, +y = 图上
ARENA_PX = (29, 31, 1252, 1254)
```

---

## 4.5 ★ 地图与建图现状 —— "要不要重新扫图"

**结论：导航不用重扫；但视频那一次 SLAM 迟早要跑，而且应该在场景冻结之后再跑。**

### 导航用的那张图是最新的

`navigation.launch` 默认 `map:=$(find competition_robot)/maps/arena_clean.yaml`，它由
`tools/gen_map_from_arena.py` **从几何生成**（不是 SLAM），`--inner` 默认已是 **2.176**：

| | 时间 | 与当前场地 |
|---|---|---|
| 场地外扩（`build_arena.py --margin 0.10`） | 09-21 12:26 | — |
| `maps/arena_clean.pgm`（导航在用） | 09-21 16:59 | ✅ 一致 |
| `competition_arena/maps/arena_map.pgm`（包内副本） | 09-21 12:18 | ✅ 一致 |

核对方法（栅格剖面）：整幅图里"可行驶区"的边界正好落在 **±2.176**，从 -2.476 到 -2.176
共 12 格（0.30 m）是墙 —— 和 `build_arena.py` 现在的输出吻合。

**道具（人偶/车/红绿灯）不是墙**：`gen_map_from_arena.py` 只画 `|x|>=inner` 或
`|y|>=inner` 的墙（可选 `--obstacles` 加内部障碍）。所以挪人偶/挪车**不需要重出地图**，
它们在运行时靠 `/scan` 进代价地图。

### 已经过期的图（别用）

| 文件 | 时间 | 问题 |
|---|---|---|
| `competition_robot/maps/arena.pgm` | 09-17 03:55 | **场地外扩之前**的 SLAM 图 |
| `maps/bench_gmapping.pgm`、`bench_karto.pgm`、`my_map.pgm` | 09-17 | 同上 |

`navigation.launch` 注释里那句"想换回 SLAM 建的那张: map:=…/arena.yaml"**现在会引入
约 10 cm 的系统性偏差**（见 §4 第 1 条）。要对比就重扫，别直接切。

### 但"建图过程"这件事逃不掉

复赛视频的硬性要求是 **"完整建图过程 + Gazebo 与 Rviz 同屏"**，导航 20 分也明确写着
"使用 Gmapping/Cartographer 等建图"。所以：

1. **等场景冻结再扫**（补完楼宇/垃圾桶/电动车等物料之后），一次搞定，不用重录；
2. 扫完把图另存（别覆盖 `arena_clean`），导航继续用几何图，视频里用 SLAM 过程；
3. 命令：`roslaunch competition_robot slam_gmapping.launch` → 跑一圈 →
   `rosrun map_server map_saver -f ~/桌面/人工智能算法大赛/maps/arena_final`。

---

## 4.6 ★ 最终巡检路线与实跑结果（2026-09-21）

### 路线怎么定的

**用户定的原则：最终路线就是原来那条巡检环线，只是沿途要停下来拍照；
两个街区的人偶识别，每个方向定一个"刚好正对"该方向人偶的点位。**

所以 `tools/gen_recognition_route.py` 不另造路线：把 10 个识别点投影到
`waypoints.yaml` 的环线上，按弧长插进航点序列，得到 **17 站**：

```
1 灯tl_top  2 人A_north  3 top_l   4 人A_west   5 mid_l    6 人A_south
7 人B_north 8 mid_cl     9 mid_c   10 灯tl_bot  11 人B_east 12 bot_c
13 牌car_3  14 bot_r     15 牌car_2 16 牌car_1   17 ring    → 回起点
```

10 个识别点**离环线都只有 0.01~0.12 m**，说明点位本来就落在巡检路径上。

### 怎么跑

```bash
tools/run_recognition_route.sh              # 一键: 起仿真 -> 跑整条 -> 存轨迹 -> 重画图
python3 tools/patrol.py --file src/competition_robot/config/recognition_route.yaml
```

`patrol.py` 的原生流程正好就是"开到固定点位原地转向拍照"：
先 cmd_vel 原地转到行进方位 → move_base 直线过去 → 到点再原地转到拍照朝向
（朝向写在 yaml 的 `yaw` 里）。**不需要额外写节点。**

### 实跑结果（本沙箱无头仿真，2026-09-21）

| 指标 | 值 |
|---|---|
| 到点 | **17/17 全部成功，0 次失败/超时** |
| AMCL 到点误差 | 均值 0.056 m，最大 0.075 m（基线 0.067 → 略好） |
| 真值误差 | 均值 0.113 m，最大 0.187 m（AMCL 本身的系统性偏差，方房间里不可观测） |
| 原地转向 | 11 次，残余全部 ≤ **1.0°** |
| 一圈用时 | **166 s**（仿真时间） |
| 轨迹 | `.cache/route/recognition_trace.csv`，实测轨迹与规划环线基本重合 |

复现图：`docs/recognition_route.png`（红线=实测轨迹，橙线=规划环线，数字=路线顺序）。

### 人偶正对点位的代价（重要，需要决策）

"正对"= 相机光轴垂直于立牌板面（正视度 1.00、横向偏置 0）→ 画面里立牌宽 123~205 px，
非常好识别。**但相机只有 0.20 m 高且无俯仰**，立牌 15 cm 高 —— 拍摄距离 d 与
画面里能看到的高度是**线性关系**：

```
可见高度 ≈ 0.366·d - 0.046   (m)      d=0.28 -> 38%   d=0.45 -> 79%   d=0.55 -> 100%
```

而车道只有 0.61 m 宽、相机还前伸 0.149 m，**车最多只能离街区边线 ~0.28 m**。

**解法（已采用）：把立牌往街区里面挪 0.20 m**（`standees.yaml` 的 `inset_m`，
摆放工具 `setup_standees.py` 读它）。拍摄距离 0.28 → **0.435 m**，
画面里能看到 **75%**（只切掉脚），需要的相机俯仰从 **17° 降到 5°**（基本不用改实车）。

> ★ **拍照点位不用改**：点位本来就卡在车道外沿（"车能站多远"的极限），
> 立牌往里挪只是**把距离拉长**，车的位置最多变 8 mm。

### 立牌背面已改纯色（附一个反直觉的标定结果）

Gazebo 的 box 会把贴图糊到**六个面**上，所以原来从背后看也是（镜像的）人物图 ——
后果有两个：① 视觉模型可能学到"正反两面都是人"，与真机域不一致；
② 从背后拍到的图会被误认为"正视目标"（调试时就吃过这个亏，绕了很久）。
现在每个立牌背面加了一块略大的纯色挡板（`gen_standees.py`）。

> ★ **反直觉的点**：挡板该放哪一面，**推导会推错**。模型的 `<pose>` 里
> `yaw=90°` 按右手系应该把局部 +x 转到世界 +y，于是 "+x 就是朝外/朝车道" ——
> 但实拍不是这样：挡板放 -x 时，车道侧看到的是**纯色板**（人偶拍不到）。
> 正确结论是 **朝车道/街区外的是模型的 -x 面**，挡板必须放 +x。
>
> 标定方法（1 分钟）：把车用 launch 参数直接生成在识别点位上，抓一帧看：
> ```bash
> roslaunch competition_robot navigation.launch gui:=false rviz:=false \
>     x:=-0.962 y:=1.852 yaw:=-1.5708
> # 另开终端抓一帧 /camera/rgb/image_raw 看
> ```
> 改立牌朝向/贴图前，**先跑一遍 `tools/capture_points.py` 核对**，别靠推。

### 到点拍照怎么用

```bash
./tools/run_recognition_route.sh --capture          # 跑整条路线 + 每个识别点拍照
./tools/run_recognition_route.sh --capture --frames 5
python3 tools/show_captures.py captures/<时间戳>     # 拼一张总览图
```

输出（每次一个文件夹）：

```
captures/2026-09-21_210643/
    run.yaml        本次元信息（路线/相机/帧数/git commit）
    index.csv       每个点: 序号/目标/位姿/真实位姿/文件名/真值/灯态/用时
    01_tl_top_f0.png ... 10 个目标 x N 帧
    overview.png    (show_captures.py 生成)
```

`index.csv` 里的 `ground_truth` 列直接可用作验收答案：车牌字符串、人偶数、
红绿灯状态（拍照瞬间的 `/traffic_light/state`）。`captures/` 已在 `.gitignore` 里。

> 相机是**按需订阅**的：Gazebo 相机在没有订阅者时不渲染，1280×960 常开渲染
> 会把仿真拖慢一倍（实测 RTF 0.66 → 0.28）。所以只在抓帧那几秒订阅。
> 无 GPU 的沙箱里一轮 10 个点约 7 分钟。

### 导航相关的两个坑（已处理）

1. **街区在仿真里没有实体墙**，只有地面画线。所以"可行驶区域"必须显式定义成一组
   矩形走廊（`tools/gen_recognition_points.py` 顶部的 `ROADS`），否则车会从街区中间穿过去。
   依据：官方示意图里中右列（x 0.21~0.83）是**楼宇A/B**、下方带是**楼宇D/站房**，都不是路。
2. **原地转向余量**。`patrol.py` 的判据是"车心离墙 ≥ `FOOTPRINT_R=0.2582`"，
   点位搜索里已经加了同一条约束，所以 dry-run 不会再出现 `✗ 转不开`；
   但外圈车道的点仍会显示 `⚠ 余量 < 0.10 m`（场地只有 4.2 m，几何上就这么多）。

---

## 5. 场景里到底摆了什么（坐标可直接抄）

### 街区（车道线围出来的区域）

| | x | y | 尺寸 |
|---|---|---|---|
| **A 街区** | [-1.472, -0.452] | [0.847, 1.468] | 1.02 × 0.62 m |
| **B 街区** | [-1.475, -0.407] | [-0.537, 0.197] | 1.07 × 0.73 m |
| 3号停车位 | [1.472, 2.076] | [-0.805, -0.197] | 0.60 × 0.61 m |
| 2号停车位 | [1.472, 2.076] | [-1.423, -0.826] | |
| 1号停车位 | [1.472, 2.076] | [-2.076, -1.448] | |

### 红绿灯（**都是横排双支架**，官方只有横排）

| 灯 | 位置 | yaw | 腿踩在哪 |
|---|---|---|---|
| `tl_top` | (-0.506, **1.779**) | 0（朝 +x） | y = 1.472 / 2.086（顶部车道两条边线） |
| `tl_bot` | (-0.110, **-1.280**) | π/2（朝 +y） | x = -0.4175 / +0.1975（竖车道两条边线） |

**两盏都是"跨在车道上方的门"，机器人从两条腿中间穿过去。** 上灯靠右的腿在墙外
（灯箱高 0.34~0.48 m，高过 0.30 m 围墙，跨墙不打架）。

`tl_bot` 原在 y=-1.456，**正好在 T 字路口上，转弯扫掠区会撞倒它**，已往里挪 0.176 m。

### 人偶立牌（10 个 = 8 社区 + F1 + F2，紧凑成组）

```
A north c01@(-1.01,+1.44) c02@(-0.92,+1.44)      A south c03@(-1.01,+0.88) c04@(-0.92,+0.88)
A west  c05@(-1.44,+1.11) F1 @(-1.44,+1.20)      B north c06@(-0.99,+0.17) c07@(-0.90,+0.17)
B east  c08@(-0.44,-0.22) F2 @(-0.44,-0.13)
```

- 朝向按示意图橙黄箭头（A 是 ←↓↑，B 是 ↑→），**每条边的人偶朝该边的法向**
- 同一方向**紧凑成组、间距 0.09 m**（识别是定点转向拍照，挤在一起才拍得全）
- 摆放工具内置**和红绿灯脚的干涉检查**（立牌是 static、灯不是，压上去会把灯顶歪）

### 车辆 + 车牌

| 模型 | 车牌 | 车位 |
|---|---|---|
| `car_1` | 苏A·B8Q62 | 3号 |
| `car_2` | 鄂D·7B5Q2 | 2号 |
| `car_3` | 苏A·PL12A | 1号 |

车尾朝 -x（朝车道）。车板 34.5×25×5 mm，车牌 9.5×3 cm 贴在**后备箱**（红框中心离车板底
11.65 cm）。

---

## 6. 仓库与提交

- 远端 `git@github.com:Gh0stown/AiC-2026.git`，分支 `main`
- **每个新终端先 `source devel/setup.bash`**（用户明确不要写进 `~/.bashrc`，留着当提醒）
- 提交身份：本仓库局部已设 `Gh0stown <728571239@qq.com>`
  （全局缺 `user.email`，不设会报"作者身份未知"而**提交失败但 push 会"成功"**——很容易漏看）
- `.gitignore` 忽略 `/build/ /devel/ /logs/ /.venv/ /datasets/ /runs/ *.pt *.onnx`
- 大文件（官方素材、视频）不要进仓库

### 关键提交（能看出演进）

```
ff98ead  人偶改紧凑成组 + 修掉立牌把红绿灯顶歪的问题
35e1e77  地图搭建完成: 加一键验收脚本 + 验收指南
1310335  车牌移到后备箱 + 相机提到 1280x960 + 修掉 OGRE 全局贴图名冲突
51719ca  人偶立牌摆进 world
0d19ab3  红绿灯: 场地外扩 0.10m 给灯留地, 彻底消除晃动
0329e48  红绿灯按官方红点重定位: 灯是"跨车道上方的门"
```

---

## 7. ★ 踩过的坑（贵的那几个，别再踩）

### 7.1 OGRE 的材质名**和贴图名**都是全局的

三个车模型都用 `Car/Body` + `Car/Plate` → 互相覆盖，**三辆车显示同一张车牌**。
只改材质名**还不够** —— `car.png`/`plate.png` 同名一样冲突。最终：
**每个模型的材质名和贴图文件名都必须唯一**（`<model>_car.png`），且生成前**清空
textures 目录**（旧文件残留会继续占名）。判断依据：抓帧看到的车牌永远是最先/最后加载
的那张。

### 7.2 立牌/道具的贴图**必须带 alpha 且用 `alpha_rejection`**

比 alpha 混合干净，没有半透明排序问题，立牌边缘就是人物轮廓。

### 7.3 静态道具用 `<static>true</static>`

立牌、车辆都设 static：不会倒、不会被碰到乱飞。**红绿灯不能 static**（要动关节），
于是它用另一套办法（见 7.4）。

### 7.4 红绿灯的晃动：结论是"**不给重力**"

排查过 5 种方案，实测记录：

| 方案 | 结果 |
|---|---|
| 开重力 + 脚压在地面 z=0 | 起步落地弹跳；之后因**零穿透接触**被求解器持续微推，**~0.1 mm/s 匀速慢漂**（23 秒 0.26 m） |
| 开重力 + 加摩擦/加重 | 无改善 |
| 灯珠质量压到 0.001 kg | 与慢漂无关：**起不起切换节点漂移速率完全一样** |
| 关节限位 `[0.005,0.055]` 而默认位置 0 在限位外 | 启动被弹一下 → 晃两下 |
| **关重力 + 整灯离地 2 mm 悬放**（当前） | **完全不动** |

代价：被撞到会被推走且不回来（所以 `tl_bot` 挪离了路口）。

### 7.5 沙箱（我这边）的三个限制 —— 影响我**能验证什么**

1. **相机渲染冻结在启动首帧**，而且取景位置**不确定**（相机插件首帧和 spawn 的竞争）。
   所以我**无法**用"把机器人放到某处再抓图"来验证渲染。
   → 设计类验证改用**离线 PIL 合成**（见 `docs/car_design.png`）。
2. **`$HOME` 只读** → Gazebo 写不了 `~/.gazebo` 就起不来。用
   `HOME=$PWD/.cache/home`（`acceptance.sh` 已自动处理）。
3. **后台任务超过 ~10 分钟会被杀**，且 `/tmp` 每个调用独立 → 长任务拆开跑，
   日志写到工作区内（`.cache/`）。

### 7.6 别的

- `RLException: ... is neither a launch file` = 没 `source devel/setup.bash`
- `check_localization.py` 的参数是 `--duration` 不是 `--samples`
- 一台 shell 里起两个 `roslaunch` 会抢 master 端口 → 先起 `robot_gazebo`，
  等 `/gazebo` 服务就绪，再起别的
- 改了 world **必须重启仿真**才生效

---

## 8. 怎么重出（改过东西之后）

| 改了什么 | 要重跑 |
|---|---|
| 场地/围墙/外扩 | `setup_traffic_lights.py && setup_standees.py && setup_cars.py && gen_map_from_arena.py` |
| 红绿灯尺寸/位置 | `python3 tools/setup_traffic_lights.py` |
| 人偶素材/摆放 | `python3 tools/gen_standees.py && python3 tools/setup_standees.py` |
| 车牌/车辆 | `python3 tools/setup_cars.py` |
| 机器人参数 | `python3 src/competition_robot/scripts/gen_robot.py` |

配置是**唯一真值源**：`config/traffic_lights.yaml`、`config/standees.yaml`、`config/cars.yaml`。
改位置改配置、别改 world（world 是生成的）。

---

## 9. 环境与路径

| | |
|---|---|
| 工作区 | `/home/ghostown/桌面/人工智能算法大赛` |
| 官方素材（**不在仓库里**） | `~/复赛资料/{人员,车辆识别,红绿灯}/` + `复赛任务示意图.png` |
| 视觉 venv | `.venv/`（torch 2.4.1+cpu / ultralytics / hyperlpr3 / opencv 4.10） |
| 主要文档 | `验收指南.md`（怎么跑）、`复赛要求与差距分析.md`（分值拆解）、`PROGRESS.md`（早期排查记录）、`src/competition_arena/docs/traffic_light_model.md`（红绿灯全部理由） |

---

## 10. 我对下一步的建议

1. **先补"识别点位"**。用户说识别是"开到固定点位原地转向拍照"——点位没定，
   后面三个视觉任务都没法收尾。定了点位顺带能算出**拍照距离 → 人偶/车牌占多少像素**，
   直接决定识别方案（模板匹配够不够、要不要上 YOLO）。
2. **再做红绿灯状态识别**（最快闭环）。相机 → HSV 找灯珠 → 判定颜色 → 发话题，
   和已有的 `/traffic_light/state` 可以交叉验证。
3. **然后人偶检测 + 计数**（分值最高、也最能体现"视觉"）。
   素材是 18 张 PNG，**Gazebo 可以自动生成合成数据集**（随机位姿截图 + 自动标注），
   不用手工标。这一步要占 GPU（用户有）。
4. **车牌 OCR** 有现成的 `plate_ocr.py`，缺的只是"从相机图里定位车牌"这一步。
   注意：**相机已提到 1280×960**，0.6 m 处车牌约 198 px（每字 26 px）够 OCR；
   1.2 m 处只有 100 px（每字 13 px）不够——**机器人得贴到车道内侧拍**。
5. **红绿灯停止线**很快，属于合规项，可以插空做。

---

## 11. 需要用户拍板的问题（还没定）

1. 实车相机分辨率是多少？（仿真现在按 1280×960 配的，是按识别需求倒推的）
2. 视觉方案走**纯 OpenCV** 还是**上 YOLO**？
3. 是否装 Cartographer？
4. 识别点位由谁定（组委会给，还是我们自己选）？
5. 报名（9-20 截止）完成了没有？

---

## 12. 已知的、但**没法在我这边确认**的事

- **视觉渲染效果**（红绿灯实际点亮的颜色、贴图有没有歪）—— 我这边相机冻结，
  只能靠"按灯珠位姿反推"和离线合成图；**建议在有 GPU 的机器上人眼过一遍**。
- **整条 `acceptance.sh` 串联运行** —— 6 项我都单独验过，但没串起来跑完过。
- **实车参数**（整车质量是估算的、雷达型号、相机安装高度/俯仰）。

### ⚠ 待查：B_east 那组立牌渲染出来是"侧看"

2026-09-21 实拍 10 个识别点，逐张核对发现：

| 组 | 画面里 | 判断 |
|---|---|---|
| A_north / A_south / A_west | 人物正面、完整，尺寸与预测一致 | ✓ |
| **B_east** | 只有一条 23 px 宽的细条（侧看） | ⚠ 需要查 |

`index.csv` 里的真实位姿是对的（B_east 真值 yaw -179.2°，与命令的 -180° 一致），
世界文件里 `B_east_1/2` 也是 `yaw=0`（朝 +x 东，正对相机）。所以**命令和配置都对**，
怀疑是 Gazebo 侧该模型的朝向/贴图问题（同类模型在别的组是正常的）。

**1 分钟复现**（带界面的机器上）：

```bash
roslaunch competition_robot navigation.launch gui:=true rviz:=false \
    x:=-0.0379 y:=-0.17 yaw:=-3.1416
# 看 Gazebo 里 B 街区东侧那两个立牌：应该正对相机
# 若确实是侧着，把 standees.yaml 里 B 街区 east 的 yaw 试 +90°（或改 EDGE 表）
```

排查入口：`tools/setup_standees.py` 的 `EDGE` 表（`east: (0.0, 'y')`）与
`tools/gen_standees.py` 里板子的轴向 —— **改完一定要用 `capture_points.py` 实拍核对**，
别靠推导（挡板那次就是推错了面）。

---

## ★ 立牌朝向/背板：一次绕了很久的坑（2026-09-21，给下次的自己）

### 结论（照这个来，别推翻）

1. **人物图渲染在模型的 `-x` 面**；`+x` 面是**透明**的（alpha 被丢弃）。
   - 标定方法（最干净）：把 world 里其它立牌全删掉、只留要测的那一组，
     再把车直接用 launch 参数生成在该点位抓帧 —— 一次就能判定，
     不用在多个组叠在一起时猜"画面里这对是谁"。
2. 所以 **yaw 要让 `-x` 指向车道/街区外**。当前 `setup_standees.py` 的 EDGE 表
   （`north=-90 / south=+90 / east=180 / west=0`）就是按这个定的，
   **已经是"用户指示的旋转 180°"之后的值**。
3. **背板（纯色挡板）必须放在 `+x` 面**（透明那一面），
   `tools/gen_standees.py` 里是 `xb = +(THICK/2 + 0.001)`。
   - 放到 `-x` = 把人物图盖住 → 车道侧看到纯色板（**看起来就像"方向反了"**，
     我因此误判过一次，来回折腾了三轮）。

### 为什么这么难查（三个叠加的坑）

* Gazebo 的 box 六个面共用一张贴图，**两面都像"人"**，靠肉眼分不清正反；
* A 街区两组（A_north / A_south）在**同一条视线上**，从车道看近的挡远的，
  很容易把"远处那组"当成"目标组"（我前两次就是这么误判的）；
* **c01 / c02（A 街区北侧那组）完全不渲染**：
  模型已 spawn（`/gazebo/get_model_state` 位姿正确）、SDF 合法、贴图有效
  （40~61% 不透明）、材质名/贴图名/脚本名都已唯一化 —— 但仍然看不见。
  所以"在 A_north 点位拍到的其实是后面 c03/c04 那对"。
  **排查入口**：`tools/setup_standees.py` 的 `EDGE` + `gen_standees.py` 的
  贴图/材质，以及带界面机器上直接看 Gazebo 里那两块板到底在不在。

### 复现 / 核对（1 分钟）

```bash
# 直接把车生成在某个识别点位上, 另开终端抓一帧
roslaunch competition_robot navigation.launch gui:=true rviz:=false \
    x:=-0.962 y:=1.861 yaw:=-1.5708
# 车道侧应看到 c01=戴草帽拿修枝剪的园艺工 / c02=抱书的眼镜男
# 看到"纯色板"或"镜像的人" -> 背板面 或 朝向 配错了
```

素材对照表（`~/复赛资料/人员/`）：
`1=园艺工(草帽) 2=抱书眼镜男 3=厨师 4=调色板女士 5=邮差 6=吉他男 7=地图男 8=黑衣女`
`F1/F2=非社区人员(兜帽人)`

---

## ★★ 最贵的一个坑：相机近裁剪面错用了深度测距下限（2026-09-21 已修）

**症状**：正对着拍立牌的点位，画面里**看不到正前方那两块**，只看到后面那组的**背面**；
换成"只能在远处看清"的位置又一切正常。看起来像"朝向反了"，其实是**根本没渲染**。

**根因**：`robot_params.yaml` 的相机 `range: [0.60, 8.0]`（Astra Pro 的**深度**测距范围）
被直接当成 RGB 相机的 `clip near/far` 用了：

```xml
<clip><near>0.6</near><far>8</far></clip>   <!-- 比 0.6m 近的东西完全不渲染 -->
```

而我们的正对点位算出来是 **0.42~0.60 m** —— 正好全在裁剪面以内 ✗。
（车牌 0.70 m、红绿灯 1.0 m 都在 0.6 m 外，所以只有人偶出问题，更难怀疑到相机。）

**修法**：相机裁剪面独立成 `clip_near: 0.05 / clip_far: 20.0`，
`gen_robot.py` 用它生成 URDF，**不再复用 `range`**。
改完立刻验过：A_north 点位拍到了 **c01 戴草帽的园艺工 + c02 抱书的眼镜男**，
全身、正对、不镜像 ✓。

**教训**：`range` 是"传感器量程"，`clip` 是"渲染裁剪"，两者是两回事，
不要图省事共用一份参数。

### 顺带确认的立牌朝向（最终版）

| 项 | 值 | 怎么定的 |
|---|---|---|
| 人物图在哪一面 | 模型 **-x** 面（+x 面透明） | 只留一组、把车生成在点位上抓帧 |
| yaw | `north=-90 / south=+90 / east=180 / west=0` | 让 -x 朝车道（即"整体 +180°"） |
| 背板（纯色） | 放 **+x** 面（透明那面） | 放错就会盖住人物图，看着像"朝向反了" |
| 材质脚本文件名 | 每模型唯一（`standee_cXX.material`） | OGRE 按文件名全局注册，同名会互相顶掉 |

**这类问题一律实拍核对，别靠推导** —— 我在这上面来回折腾了三次。
