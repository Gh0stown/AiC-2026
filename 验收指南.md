# 验收指南 —— 怎么启动、怎么判定通过

> 场景（场地 + 红绿灯 + 人偶立牌 + 车辆车牌）已经搭建完成。
> 这份文档说明怎么把它跑起来、怎么验收。

---

## 一、最少步骤（三条命令）

```bash
cd ~/桌面/人工智能算法大赛

catkin_make                       # 只在改过 C++/msg 或新加脚本后需要
source devel/setup.bash           # ★ 每个新终端都要 source

roslaunch competition_robot navigation.launch
```

`navigation.launch` 会一次性拉起**全套**：

| 组件 | 说明 |
|---|---|
| Gazebo 场地 | 4.4×4.4 m 地板，白线内 4.2×4.2 可行驶，围墙 0.30 m |
| 红绿灯 | 两盏**横排双支架**灯（跨在车道上，车从两条腿中间过） |
| 红绿灯切换节点 | 绿 6s → 黄 2s → 红 6s 循环（已并入启动文件） |
| 人偶立牌 | 10 个（A 街区北/南/西，B 街区北/东） |
| 车辆+车牌 | 3 辆（右下角三个停车位） |
| 机器人 + RViz | 含相机 1280×960、雷达、IMU、轮式里程计 |
| 定位/导航 | map_server + AMCL + move_base(DWA) |

只要看画面的话，直接 `roslaunch competition_robot robot_gazebo.launch` 也行。

---

## 二、一键验收（推荐）

```bash
./tools/acceptance.sh              # 无头全自动, 约 12 分钟
./tools/acceptance.sh --quick      # 跳过最长的巡逻, 约 4 分钟
./tools/acceptance.sh --gui        # 带图形界面, 人工一起看
./tools/acceptance.sh --keep       # 跑完不关, 留着手动操作
```

它会自动跑完 6 项检查，最后打印"通过/失败"汇总，并返回退出码（全过 = 0）。
所有原始日志在 `.cache/acceptance/`。

### 6 项检查与判定标准

| # | 检查 | 判定标准 | 怎么测的 |
|---|---|---|---|
| 1 | **场景完整性** | 场地/围墙/机器人/2 灯/10 人偶/3 车 全部加载 | 查 `/gazebo/get_world_properties` |
| 2 | **红绿灯切换** | 两盏灯 × 3 色 = **6/6 正确** | 按灯珠位姿反推（不依赖渲染） |
| 3 | **定位精度** | AMCL 平均位置误差 **< 0.10 m** | AMCL vs `/odom_groundtruth` |
| 4 | **雷达位姿标定** | 激光反推误差 **< 15 mm** | 用 `/scan` 拟合两面墙 |
| 5 | **巡航** | **7/7 航点**到达 + 全程 **0 帧**压车道线 | 跑标准 7 航点 |
| 6 | **倒车入库** | 位置误差 **< 3 cm** | 车尾入库到位姿容差 |

### 预期结果（我这边实测过的基线值）

```
定位      位置误差 均值 ≈ 0.067 m      (阈值 0.10)
雷达位姿  激光 2~5 mm  vs  AMCL 13.4 mm (激光比 AMCL 准 4 倍)
巡航      7/7, 全程 0 帧压线
倒车入库  位置 1.03~1.08 cm, 角度 0.01~0.04°
红绿灯    6/6
```

---

## 三、分步验收（想逐项自己看时）

每项都能单独跑。**先把仿真起起来**（另开一个终端）：

```bash
source devel/setup.bash
roslaunch competition_robot robot_gazebo.launch
```

然后在**另一个终端**（同样先 `source devel/setup.bash`）：

```bash
# 1) 场景: 数一数模型
rosservice call /gazebo/get_world_properties | grep -oE "'(tl_|A_|B_|car_)[^']*'" | sort

# 2) 红绿灯: 6 个状态逐个验证
rosrun competition_arena traffic_light.py &     # 或 roslaunch 已带
rostopic echo /traffic_light/state               # 看它自己报的状态
python3 tools/verify_traffic_light.py            # 结论: ✓ 6/6 全部正确
#   手动强制某个状态:
rostopic pub -1 /traffic_light/command std_msgs/String "data: 'red'"
rostopic pub -1 /traffic_light/command std_msgs/String "data: 'auto'"

# 3) 定位精度
python3 tools/check_localization.py --duration 30

# 4) 雷达位姿标定
python3 tools/check_laser_pose.py --n 12

# 5) + 6) 巡航 + 倒车入库 (约 8 分钟)
python3 tools/patrol.py --save-trace .cache/trace.csv
python3 tools/plot_trace.py .cache/trace.csv      # 画轨迹图
```

---

## 四、人工要看的（自动脚本查不了）

跑 `./tools/acceptance.sh --gui`，或单独 `roslaunch competition_robot navigation.launch`，
然后在 Gazebo / RViz 里确认：

- [ ] **红绿灯**朝向我们、三颗灯珠都看得见；绿/黄/红按 6/2/6 秒循环
- [ ] **红绿灯的腿**踩在车道白线上，机器人从两条腿中间穿过去
- [ ] **人偶立牌**贴图正确、是人物轮廓（不是一块方块）
- [ ] **车牌**贴在**后备箱**上、清晰可读（三个车位分别是 苏A·B8Q62 / 鄂D·7B5Q2 / 苏A·PL12A）
- [ ] **车辆** 34.5×25 cm、人偶 15 cm —— 和真车/真人比例合理
- [ ] 机器人跑巡航时**不压越车道线**
- [ ] RViz 里 `/scan`、`/camera/rgb/image_raw`、TF 树都正常

相机画面上踩过的坑：RViz 里如果 `Image` 不显示，选 `camera` → `/camera/rgb/image_raw`，
并确认 **Fixed Frame** 是 `map` 或 `base_footprint`。

---

## 五、出问题先看这里

| 现象 | 原因 / 处理 |
|---|---|
| `RLException: ... is neither a launch file` | 没 `source devel/setup.bash` |
| 找不到 `competition_arena` 的模型 | `catkin_make` 后重开终端；或 `GAZEBO_MODEL_PATH` 没带上 |
| 地图和场地对不上（墙位置偏 10 cm） | 地图没重出：`python3 tools/gen_map_from_arena.py` |
| 红绿灯三盏全亮 / 全灭 | 材质名或**贴图名**被别的模型覆盖了（OGRE 全局注册），查模型目录里贴图名是否唯一 |
| 门过不去 / DWA 一直 failed | `costmap_common_params.yaml` 里 `inscribed_radius` 要 ≤ 0.10 |
| 人偶或车牌显示成别的图 | 同上，**贴图文件名必须每个模型唯一** |
| gzserver 段错误 | 偶发，重跑一次；持续出现看 `.cache/acceptance/sim.log` |

---

## 六、改过东西之后要重出什么

| 改了什么 | 要重跑 |
|---|---|
| 场地/围墙/外扩（`build_arena.py`） | `python3 tools/setup_traffic_lights.py && python3 tools/setup_standees.py && python3 tools/setup_cars.py && python3 tools/gen_map_from_arena.py` |
| 红绿灯尺寸/位置 | `python3 tools/setup_traffic_lights.py` |
| 人偶素材/摆放 | `python3 tools/gen_standees.py && python3 tools/setup_standees.py` |
| 车牌/车辆 | `python3 tools/setup_cars.py` |
| 机器人参数（`robot_params.yaml`） | `python3 src/competition_robot/scripts/gen_robot.py` |

> 改动 world 之后**必须重启仿真**才生效。
